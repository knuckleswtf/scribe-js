import { scribe } from "@knuckleswtf/scribe";
declare function run(endpoint: scribe.Endpoint, config: any): scribe.UrlParameters;
declare const _default: {
    routers: string[];
    run: typeof run;
};
export = _default;
