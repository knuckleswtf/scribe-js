# {{{ groupName }}}
{{{ groupDescription }}}

{{#each endpoints}}
{{> partials.endpoint endpoint=this exampleLanguages=../settings.exampleLanguages baseUrl=../settings.baseUrl auth=../auth settings=../settings}}
{{/each}}

